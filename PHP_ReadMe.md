// ==================================================
# PHPの特徴
// ==================================================

## PHPの文字列はシングルクォートで記述

## ダブルクォートを使った文字列
*   タブや改行といった特別な文字を表すエスケープシーケンスという記法を処理したり、文字列内に記述した変数の値を展開したり、といった機能があります。

## PHPのプログラムはスクリプト又はPHPスクリプトと呼称する

## PHPスクリプトを実行するときは、スクリプトのURLを指定します

## 浮動小数点数
*   PHPで除算を行うと、割り切れない部分には、小数部分がある数値になります。小数部分がない数値のことを整数、少数部分がある数値のことを実数または浮動小数点数と呼ぶことがあります。

<!-- 段落 -->

## 文字コードは授業ではutf-8を使用する
*   文字コードの設定が違っていると文字化けを起こすことがあります。

### 文字コード
*   コンピュータで文字を扱うために、文字に対して割り当てた数値のことを、文字コードと呼びます。

### 文字エンコード
*   文字に対して割り当てた数値をコンピュータで実際に扱うデータにする方法のことを、文字エンコーディングと呼びます。

### utf-8 
*   UTF-8は、Unicodeと呼ばれる文字コードに対する、文字エンコーディングの一種です。

<!-- 段落 -->

## HTMLとPHPの使い分け
*   HTMLとPHPは混在されることができます。両者は次の様に使い分けるのがおすすめです。
-   HTML：常に同じ出力を行う
-   PHP：状況に応じて異なる出力を行う

## PHP構文の入力補助設定を有効にする
*   １．VScodeの画面左下にある歯車アイコンを押下して「設定」を開く
*   ２．拡張機能の中にある「PHP」を押下
*   ３．項目「PHP > Validate: Executable Path」の「setting.jsonで編集」を押下
*   ４．開かれたjsonファイルの項目「php.validate.executablePath」の右側にある""の中に、下記内容を追記
-   C:\\xampp\\php\\php.exe


## PHPはXAMPP　又は　MAMPを使用する
*   XAMPPは「X：クロスプラットフォーム」「A：Apache」「M：MariaDB/MySQL」
*   「P:PHP/Perl/Python」を意味します。
*   MAMPは「M:macOS」「A:Apache」「M:MariaDB/MySQL」「P:PHP/Perl/Python」を意味します。
    
*   PHPスクリプトを実行するには本来、クライアントサイドとサーバサイド両方のコンピュータが必要です。しかし、XAMPP/MANPを利用することで、ネットワークを介してサーバと通信したり、サーバにファイルを転送したりすることなく、現在使用しているコンピュータだけで開発が行えます。
*   XAMPP/MAMPをインストールすれば、Webサーバやデータベース管理システムなど、Webアプリケーションの開発に必要なソフトウェアの一式が、すべて手元にあるコンピュータで動きます。

// ==================================================
# XAMPP/MANPの使用上の注意
// ==================================================

*   XAMPPを終了せずにそのままPCの電源を切ったり、XAMPPコントロールパネル右上の×ボタンを押して閉じたりせずに、必ず、XAMPPコントロールパネルにある「Quit」　か、右下のメニュー上にあるXAMPPアイコンを右クリックした中にある「Quit」を押下して閉じること、この方法で閉じないと、XAMPPの再インストールなどが必要になることがある
    
*   表示されたエクスプローラー上から「xampp-control.ini」を探して右クリックし「プロパティ」をクリック表示されたダイアログ上のタブ「セキュリティ」にある「編集」を押下、上部の「Evryone」を選択し、下部の「フルコントロール」のチェックボックスにチェックを入れる

// ==================================================
# PHPで主に使用される記述群
// ==================================================

## <?php ~ ?>
*   PHPタグ
*   この中にスクリプトを記述する
*   HTMLの<html></html>の様なもの
*   終了タグを省略することができます。

## ヒアドキュメント構文
*   ヒアドキュメントは、複数行の文字列を表現するための構文です。echoやprintと組み合わせて、複数行のメッセージを出力することができます。
*   echoやprintと記述したあと<<<の後に指定した記号が、文字列の終わりを表します。
-   以下、例
        echo <<<END
        Welcome
        to
        PHP
        END;

## echoタグ
*   <?php echo 'いらっしゃいませ'; ?>のように、echoを１つだけ含むPHPスクリプトは、次のような短い記法で書くこともできます。この記法はechoタグと呼ばれます。HTML部分の中に、少しだけPHPで出力する部分が混じっている場合などに便利な記法です。
-   以下、例
       <? 'いらっしゃいませ' ?>

<!-- 段落 -->

## 値を画面に出力する

### echo 'メッセージ';
-   戻り値がない
-   複数の値を出力できる
-   少し早く出力される

### print 'メッセージ';
-   戻り値がある
-   基本的に１つだけ出力する
-   少し遅い

<!-- 段落 -->

## require 'ファイルパス'
*   複数のスクリプトで、同じ部分を使いまわすことがあります。このような場合は、使いまわす共通部分を別のファイルに保存しておき、スクリプトから読み込むようにします。この方法には以下のような利点があります。
    　
*   繰り返し同じ内容を入力する手間が省け、スクリプトが簡潔になります。

*   共通部分を一括して変更したくなった場合に、複数のスクリプトを１つひとつ変更する必要がなく、共通部分のファイルだけを変更すれば済みます。

## フォーム送信時の動作
*   出力用のスクリプトは<form>タグのaction属性に指定します。

## 定数
*   定数は値に名前を付ける機能です。使い方は変数に似ていますが、変数とは違い、最初に値を設定したら、再び値を設定し直すことができません。定数は以下のように、constというキーワードを使って定義します。

### const 定数名 = 値
*   変数とは違い、定数の先頭に$記号は付けません。

<!-- 段落 -->

## 変数
*   値を格納するための仕組みです。適切に変数を使用すると、スクリプトが簡潔になったり、理解しやすくなったり、複雑な計算が記述できるようになったり、といった効果があります。
*   PHPの変数は、変数名の前に$記号をつけます。
*   １文字目は英字またはアンダースコア(_)を使う
*   ２文字目以降は英字、数字、アンダースコアのいずれかを使う
*   英字の大文字と小文字は区別される

## すでに定義されている変数

### $_REQUEST
*   HTTPのリクエストパラメータ一式（GETおよびPOST）

## $-REQUEST['リクエストパラメータ名']
*   コントロールのname属性の値がリクエストパラメータ名となります。
*   $-REQUESTにはリクエストパラメータの値が格納されます。

### $_GET
*   HTTPのGETリクエストパラメータ

### $_POST
*   HTTPのPOSTリクエストパラメータ

### $_FILES
*   アップロードされたファイルの情報

### $_SESSION
*   セッション

### $_COOKIE
*   クッキー

<!-- 段落 -->

##　配列
*   配列は、複数の値をまとめて管理するための機能です。変数には１つの値だけを格納しますが、配列には複数の値を格納できます。
*   リクエストパラメータで利用する$_REQUESTも実は配列であり、複数の値を格納することが可能です。
*   配列内には、複数の値を格納するために、複数の領域があります。この領域のことを、配列の<mark>要素</mark>といいます。

## isset

## htmlspecialchars()
*   関数の一種でHTMLにおいて特別な動きをする文字について、働きを無効にします。ユーザーが入力した文字列を表示したり、保存したりする際に、安全性を高める効果があります。

// ==================================================
# 演算子
// ==================================================

## **   累乗

## ++   １を加算

## --   １を減算

## !    論理演算子（否定）

## *    乗算演算子

## /    除算演算子

## %    剰余演算子

## +    加算演算子

## -    減算演算子

## .    文字列結合

##      比較演算子

### <   より小さい

### <=  以下

### >   より大きい

### >=  以上

### == or === 等しい

### != or !== 等しくない

##      論理演算子

### &&   かつ　両方

### ||  または　どちらか

## =    代入演算子
*       =を代入演算子と呼びます。代入演算子を使うと、左辺の変数に、右辺の値が書き込まれます。

// ==================================================
# error 一覧
// ==================================================

## Fatal error
*   致命的なエラー

### Uncaught error : 内容
*   キャッチされないエラー