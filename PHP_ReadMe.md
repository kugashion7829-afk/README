# PHPの特徴

## PHPの文字列はシングルクォートで記述

## ダブルクォートを使った文字列
*   タブや改行といった特別な文字を表すエスケープシーケンスという記法を処理したり、文字列内に記述した変数の値を展開したり、といった機能があります。

### 変数の展開について
*   PHPでは、シングルクォートで囲うことが多い。だがシングルクォートの特性上シングルクォートの中の変数は展開されない。そのため記述のなかでシングルクォートを閉じ変数を展開した後またシングルクォートを記述する必要がある。

```
for ($i=0; $i<10; $i++) {<br>
    echo '<option value="', $i,'">', $i, '</option>';<br>
}
```

## エスケープシークエンス   
### `\n`
*   改行

### `\t`
*   タブ

### `\"`
*   "を文字として扱う

### `\\`
*   \を文字として扱う

## PHPのプログラムはスクリプト又はPHPスクリプトと呼称する

## PHPスクリプトを実行するときは、スクリプトのURLを指定します

## 浮動小数点数
*   PHPで除算を行うと、割り切れない部分には、小数部分がある数値になります。小数部分がない数値のことを整数、少数部分がある数値のことを実数または<mark>浮動小数点数</mark>と呼ぶことがあります。

<!-- 段落 -->

## 文字コードは授業ではutf-8を使用する
*   文字コードの設定が違っていると文字化けを起こすことがあります。

### 文字コード
*   コンピュータで文字を扱うために、文字に対して割り当てた数値のことを、<mark>文字コード</mark>と呼びます。

### 文字エンコード
*   文字に対して割り当てた数値をコンピュータで実際に扱うデータにする方法のことを、<mark>文字エンコーディング</mark>と呼びます。

### utf-8 
*   UTF-8は、Unicodeと呼ばれる文字コードに対する、文字エンコーディングの一種です。

<!-- 段落 -->

## HTMLとPHPの使い分け
*   HTMLとPHPは混在されることができます。両者は次の様に使い分けるのがおすすめです。
-   HTML：常に同じ出力を行う
-   PHP：状況に応じて異なる出力を行う

## PHP構文の入力補助設定を有効にする
*   １．VScodeの画面左下にある歯車アイコンを押下して「設定」を開く
*   ２．拡張機能の中にある「PHP」を押下
*   ３．項目「PHP > Validate: Executable Path」の「setting.jsonで編集」を押下
*   ４．開かれたjsonファイルの項目<mark>「php.validate.executablePath」</mark>の右側にある""の中に、下記内容を追記
-   C:\\xampp\\php\\php.exe


## PHPはXAMPP　又は　MAMPを使用する
*   XAMPPは「X：クロスプラットフォーム」「A：Apache」「M：MariaDB/MySQL」
*   「P:PHP/Perl/Python」を意味します。
*   MAMPは「M:macOS」「A:Apache」「M:MariaDB/MySQL」「P:PHP/Perl/Python」を意味します。
    
*   PHPスクリプトを実行するには本来、クライアントサイドとサーバサイド両方のコンピュータが必要です。しかし、XAMPP/MANPを利用することで、ネットワークを介してサーバと通信したり、サーバにファイルを転送したりすることなく、現在使用しているコンピュータだけで開発が行えます。
*   XAMPP/MAMPをインストールすれば、Webサーバやデータベース管理システムなど、Webアプリケーションの開発に必要なソフトウェアの一式が、すべて手元にあるコンピュータで動きます。

# XAMPP/MANPの使用上の注意

*   XAMPPを終了せずにそのままPCの電源を切ったり、XAMPPコントロールパネル右上の×ボタンを押して閉じたりせずに、必ず、XAMPPコントロールパネルにある「Quit」　か、右下のメニュー上にあるXAMPPアイコンを右クリックした中にある「Quit」を押下して閉じること、この方法で閉じないと、XAMPPの再インストールなどが必要になることがある
    
*   表示されたエクスプローラー上から「xampp-control.ini」を探して右クリックし「プロパティ」をクリック表示されたダイアログ上のタブ「セキュリティ」にある「編集」を押下、上部の「Evryone」を選択し、下部の「フルコントロール」のチェックボックスにチェックを入れる

# PHPで主に使用される記述群

## <?php ~ ?>
*   PHPタグ
*   この中にスクリプトを記述する
*   HTMLの<html></html>の様なもの
*   終了タグを省略することができます。

## ヒアドキュメント構文
*   ヒアドキュメントは、複数行の文字列を表現するための構文です。echoやprintと組み合わせて、複数行のメッセージを出力することができます。
*   echoやprintと記述したあと<mark><<<</mark>の後に指定した記号が、文字列の終わりを表します。
-   以下、例
```
        echo <<< END <br>
        Welcome<br>
        to<br>
        PHP<br>
        END;
```

## `<form></form>`

### `<input></input>`
#### `<input type="text">`
*   テキストボックス

#### `<input type="radio">`
*   ラジオボタン

#### `<input type="submit">`
*   決定ボタン

#### `<input type="checkbox">`
*   チェックボックス
*   チェックボックスのname属性に[]をつけると、配列を使って複数の値を取得できます。

#### `<input type="password">`
*   <input>タグのtype属性をpasswordにするとパスワード入力欄になります。
*   <input>タグのtype属性をpasswordとtextを切り替えることで入力されたパスワードの可視性を切り替えることができます。

#### `<inpur type="file">`
*   <input>タグのtype属性をfileにするとファイル選択欄を作成できます。

### `<select></select>`
*   セレクトボックス
*   セレクトボックスの選択肢を追加するには、<select></select>の間に、<option>タグを記述します。

#### `<option></option>`
*   セレクトボックスの中身

### action属性
*   出力用のスクリプトは<form>タグのaction属性に指定します。

### name属性
*   name属性の値はリクエストパラメータ名として設定されます。
*   名前が同じラジオボタンはグループになります。

### checked属性
*   checkedは、そのラジオボタンをあらかじめ選択状態にしておくことができます。

### value属性
*   value属性に設定された値は、スクリプトから取得することができます。この値を使ってどのラジオボタンが選択されたのか調べます。
*   どのラジオボタンが選択されているかは、リクエストパラメータを使って取得することができます。ラジオボタンは、name属性に設定した値がリクエストパラメータ名となります。そしてリクエストパラメータには、<mark>選択したラジオボタンのvalue属性の値が設定されます。</mark>
*   $_REQUEST['name属性']という式で、選択されたラジオボタンのvalue属性を取得できます。

### method属性
#### `post`
*   method属性のpostというのは、HTTPにおいて、フォームの内容をサーバに送信するための方式です。

#### `get`
*   一方、method属性にgetを指定することもできます。getは本来、サーバからファイルなどを取得するために用いるのですが、ファームの内容をサーバに送信する際にも指定することができます。

##### データの確認
*   WEB上のコンソールログのペイロードで送られているデータを確認できる

### enctype属性
*   enctypeは、MIMEタイプを指定するための属性です。
*   MIME(Multipurpose Internet Mail Extension)とは、データの種類を表現する方法や、データを変換する方法を定めた規格です。もともとはメールでいろいろなデータを扱うための企画ですが、Webでも使用されています。

#### `multipart/form-data`
*   multipart/form-dataは、HTTPでファイルをアップロードする際に用いるMIMEタイプです。multipartは複数のファイルをまとめるための形式で、メールでは本文と添付ファイルをまとめるために使います。HTTPではフォームへの入力内容とアップロードするファイルをまとめるために使います。

## echoタグ
*   <?php echo 'いらっしゃいませ'; ?>のように、echoを１つだけ含むPHPスクリプトは、次のような短い記法で書くこともできます。この記法はechoタグと呼ばれます。HTML部分の中に、少しだけPHPで出力する部分が混じっている場合などに便利な記法です。
-   以下、例
       <? 'いらっしゃいませ' ?>

<!-- 段落 -->

## 値を画面に出力する

### `echo 'メッセージ';`
*   戻り値がない
*   複数の値を出力できる
*   少し早く出力される

### `print 'メッセージ';`
*   戻り値がある
*   基本的に１つだけ出力する
*   少し遅い

##  値を確認する（デバッグ）

### `print_r`
*   用途：  配列・オブジェクトの中身を確認しやすい
*   情報量：中程度
*   視認性：情報が簡潔

### `var_dump`
*   用途：  型や値を詳しく確認できる
*   特徴：  データ型の確認ができる
*   情報量：多い
*   視認性：情報量が多い分少し難解

<!-- 段落 -->

## `require 'ファイルパス'`
*   複数のスクリプトで、同じ部分を使いまわすことがあります。このような場合は、使いまわす共通部分を別のファイルに保存しておき、スクリプトから読み込むようにします。この方法には以下のような利点があります。
    　
*   繰り返し同じ内容を入力する手間が省け、スクリプトが簡潔になります。

*   共通部分を一括して変更したくなった場合に、複数のスクリプトを１つひとつ変更する必要がなく、共通部分のファイルだけを変更すれば済みます。

## 定数
*   定数は値に名前を付ける機能です。使い方は変数に似ていますが、変数とは違い、最初に値を設定したら、再び値を設定し直すことができません。定数は以下のように、constというキーワードを使って定義します。変数とは違い、定数の先頭には$を付けません。また、定数名の規則は変数名と同様ですが、変数と区別するために、大文字を使う習慣があります。

### const 定数名 = 値
*   変数とは違い、定数の先頭に$記号は付けません。

<!-- 段落 -->

## 配列
*   配列は、複数の値をまとめて管理するための機能です。変数には１つの値だけを格納しますが、配列には複数の値を格納できます。
*   リクエストパラメータで利用する$_REQUESTも実は配列であり、複数の値を格納することが可能です。

### 要素
*   配列内には、複数の値を格納するために、複数の領域があります。この領域のことを、配列の<mark>要素</mark>といいます。

### キー
*   個々の要素を区別して操作するためには、<mark>キー</mark>という機能を使います。例えば、リクエストパラメータの取得の際に使用する$_REQUEST['user']などの記述において、'user'の部分がキーです。キーには整数または文字列を使います。

### キーと値
*   PHPの配列には、<mark>キー</mark>と<mark>値</mark>の組を格納する機能があります。この仕組みを利用することで、キーを指定すると組になった値を取得することができます。
*   配列にキーと組み合わせを格納するには、<mark>=></mark>を使って次のように記述します。
```
変数 = [<
    キーA => 値A,
    キーB => 値B,
    キーC => 値C,
];
```

### 連想配列
*   多くのプログラミング言語において、配列の要素を指定するには「0,1,2,....」といった整数を使います。このような整数を<mark>添字</mark>(そえじ)と呼びます。一方で、整数のかわりに文字列を使って要素を指定できるようにした配列のことを、<mark>連想配列</mark>と呼びます。

### array関数
*   $color = array('ホワイト','ブルー','レッド','イエロー','ブラック',);
*   配列は上記のようなarray関数を使っても出力できます。

### 配列への追加
`配列[] = 新規要素`
*   配列内にある、個々の値を格納する領域のことを、配列の<mark>要素</mark>と呼びます。上記の構文を使うと、配列の末尾に、新しい要素を追加することができます。

<!-- 段落 -->

## 変数
*   値を格納するための仕組みです。適切に変数を使用すると、スクリプトが簡潔になったり、理解しやすくなったり、複雑な計算が記述できるようになったり、といった効果があります。
*   PHPの変数は、変数名の前に<mark>$記号</mark>をつけます。
*   １文字目は英字またはアンダースコア(_)を使う
*   ２文字目以降は英字、数字、アンダースコアのいずれかを使う
*   英字の大文字と小文字は区別される
*   変数には数値や文字列などの値を代入することができます。数値を代入した変数は、スクリプト内で数値の代わりとして扱うことができます。文字列を代入すると、文字列の代わりとなります。なお、スクリプト内に記述した数値や文字列の値のことを「<mark>リテラル</mark>」と言います。

## すでに定義されている変数

### $_REQUEST
*   HTTPのリクエストパラメータ一式（GETおよびPOST）

#### $-REQUEST['リクエストパラメータ名']
*   コントロールのname属性の値がリクエストパラメータ名となります。
*   $-REQUESTにはリクエストパラメータの値が格納されます。

### $_GET
*   HTTPのGETリクエストパラメータ

### $_POST
*   HTTPのPOSTリクエストパラメータ

### $_FILES
*   アップロードされたファイルの情報

### $_SESSION
*   セッション

### $_COOKIE
*   クッキー

<!-- 段落 -->

## 条件分岐
### if (条件) { 条件が成立したときの処理; }
*   通常、PHPスクリプトは上に書かれた行(文)から順番に実行されます。上から順にすべての行を実行していき、最後の行が実行されると終了となります。「ボタンが選択されたらメッセージを表示する」といった処理であればこの仕組みで十分なのですが、「チェックボックスの状態によって、表示するメッセージを変化させる」といった処理を作りたい場合は、上の行から順番に実行していくだけでは実現できません。
*   そこで、ユーザーの選択内容などによってスクリプトの流れを変化させる、<mark>制御構造</mark>という仕組みを使います。
*   条件に応じて処理を分岐させることを<mark>条件分岐</mark>と呼びます。

### if (条件) { 条件が成立したときの処理; } else { 条件が成立しなかったときの処理; }
*   if-else文を使えば、チェックの有無によって処理を変えることができます。

#### 真偽値
*   <mark>真偽値</mark>というのは、条件の成立を不成立を表す値です。真偽値には<mark>TRUE</mark>と<mark>FALSE</mark>があります。
*   TRUEは条件が成立したことを表す値です。FALSEは条件が成立しなかったことを表す値です。true,falseは大文字と小文字が区別されません。

### switch文
*   ユーザーの選択内容に応じて、実行される処理を何通りかに分岐したいことがあります。if文やif-else文を用いて記述することもできますが、<mark>switch</mark>文を使用することで、より簡潔に記述することができます。

```
switch (式) {
    case 値A:
    式の値が値Aの時の処理;
    break;
    case 値B:
    式の値が値Bの時の処理;
    break;
    case 値C:
    式の値が値Cの時の処理;
    break;
}
```

*   switch文の{}の中には、複数の<mark>case</mark>文を並べることができます。switch文の式の値がcase文に記述した値に等しいとき、そのcase文の処理を実行します。
*   case文の末尾には<mark>break</mark>文を記述します。break文は処理を終了して、switch文を抜け出します。break文を記述しない方法がありますが、最初のうちは記載する方がおすすめです。

### default
*   defaultはcase文の特別な場合で、<mark>どのcase文にも該当しなかったとき</mark>に実行されます。
*   defaultを記述する場合は、switch文の{}内の最後に配置することが一般的です。defaultにも、他のcase文と同様に、break文をつけるのがおすすめです。

```
switch (式) {
    case 値A:
    式の値が値Aの時の処理;
    break;
    case 値B:
    式の値が値Bの時の処理;
    break;
    default:
    式の値がどのcase文にも該当しないときの処理;
    break;
}
```

### `isset(変数)`
*   <mark>isset関数は、変数に値が代入されていて、かつ値がNullでないときに、trueを返します。</mark>

### if文の条件にリクエストパラメータを使用する
*   if(isset(リクエストパラメータの変数)){ 変数が定義されているときの処理; }

### if-else文の条件にリクエストパラメータを使用する
*   if(isset(リクエストパラメータの変数)) { 変数が定義されているときの処理; } else { 変数が定義されていないときの処理; }

### if文を入れ子にする
*   if文の中にif文を記述する。このように、制御構造の中に制御構造を含めることを、「<mark>入れ子</mark>」または「<mark>ネスト</mark>」と呼びます。if文だけではなく、if-else文、forループ、whileループなども、入れ子にすることができます。
```
    if(is_uploaded_file(...)) {
        if(!file_exists(...)) {
            ...
        }
        ...
    }
```

## 式
*   式というのは、<mark>値、変数、演算子、関数を組み合わせたもの</mark>です。例えば、「１＋２」という式は、値；１　演算子：+　値：２を組み合わせたものです。
*   式を計算することを、<mark>式を評価する</mark>と呼びます。上記の式を評価すると「３」という結果が決まります。このように式全体が表す値のことを、<mark>式の値</mark>と呼びます。つまり、「１＋２」という式の値は「３」です。

### if (式) { 式の値がtrueのときの処理; }
*   if文やif-else文の条件には、式を記述することができます。if文は、<mark>式を評価した結果として、式の値がtrueならば、{}ないの処理を実行する</mark>構文だと言えます。

### if (式) { 式の値がtrueのときの処理; } else { 式の値がfalseのときの処理; }
*   if-else文は、<mark>式を評価した結果として、式の値がtrueならばif側の{}内を、falseならばelse側の{}内を実行する</mark>構文だと言えます。

#### 中括弧{}の省略
*   if文では、中括弧{}内の処理が１つ分だけの場合、{}を省略することができます。{}内の処理が複数の場合には、{}を省略することはできません。

<!-- 段落 -->

## ループ
*   スクリプトにおいて、<mark>指定した処理を指定した回数だけ繰り返したい</mark>ことがあります。このような繰り返しのことを、<mark>ループ</mark>と呼びます。ループも条件分岐と同様に、制御構造の１つです。

##### 開始処理
*   forループの開始時、最初に一度だけ行う処理です。通常は、繰り返し処理に使う変数の初期値を設定します。

##### 条件式
*   繰り返しを行うかどうかを判定するための式を設定します。条件式の結果がtrueの間は繰り返しを行います。falseになったら繰り返しを終了します。

##### 更新処理
*   繰り返しのたびに行う処理です。通常は、繰り返す処理に使う変数の値を増減します。変数の値が変化することで、条件式の結果も変わってきます。

### forループ
*   whileループは条件式がtrueの間は処理を繰り返します。
```
for (開始処理; 条件式; 更新処理) {<br>
    繰り返し処理;<br>
}
```

### whileループ
```
while (条件式) {<br>
    繰り返し処理;<br>
}
```

```
開始処理;
while (条件式) {
    繰り返し処理;<br>
    更新処理;<br>
}
```

### foreachループ
*   foreachループは配列に格納されたすべての値に対して繰り返し処理を行います。
```
foreach (配列 as 変数) { <br>
    変数を使った処理;<br>
}
```

### foreachループを使ったキーと値の取り出し
*   配列に格納した店舗名と店舗コードを１つずつ取り出して、以下のような<option>タグを生成します。
`<option value="店舗コード">店舗名</option>`
*   配列では、店舗名がキー、店舗コードが値です。これらを取り出すには、foreachループを次のように記述します。前述したforeachループの書式との違いは、asの後を「変数」ではなく「<mark>キーの変数 => 値の変数</mark>」とするところです。
```
    foreach(配列 as キーの変数 => 値の変数) {
        キーの変数と値の変数を使った処理;
    }
```

<!-- 段落 -->

## 関数
### 関数の呼び出し
*   関数を実行することを、<mark>関数を呼び出す</mark>と表現します。関数を呼び出すときには以下のような記述をします。
*   関数は<mark>引数</mark>を渡して実行し、実行結果は<mark>戻り値</mark>として呼び出し元に渡されます。
`関数名(引数)`

### 引数
*   <mark>引数</mark>は関数に渡す情報です。関数は引数を受け取って、その情報を演算や表示などに使います。<mark>同じ関数の呼び出しでも引数の値が異なれば動作が変わる</mark>ことが一般的です。<br>
    関数によって、引数の個数や、各引数の役割が決まっています。引数が複数ある場合には、以下のように「,」で縊って記述します。
`関数名(引数1,引数2,......)`

### 戻り値　または　返り値
*   関数の実行が終了すると、関数は処理の結果の値を呼び出し元に返します。この値のことを<mark>戻り値</mark>と呼びます。

### `htmlspecialchars()`
*   関数の一種でHTMLにおいて特別な動きをする文字について、働きを無効にします。ユーザーが入力した文字列を表示したり、保存したりする際に、安全性を高める効果があります。

## date関数
### `date(フォーマット)`
*   date関数は<mark>現在の日時を取得し、フォーマットで指定した形式に基づいて整形し、文字列として返します。</mark>

### フォーマット
*   例えば、date('Y/m/d H:i*s')のような関数の呼び出しは、「２０２６/０８/２８ １３：１６：４９」のような文字列を返します。一方、date('Y年m月d日 H時i分s秒')のような呼び出しは、「２０２６年０８月２８日 １３時１６分４９秒」のような文字列を返します。

#### `Y`
*   年。４桁の数字
#### `m`
*   月。２桁の数字。先頭にゼロを付ける
#### `d`
*   日。２桁の数字。先頭にゼロを付ける
#### `H`
*   時。２桁の数字。先頭にゼロを付ける。２４時間単位
#### `i`
*   分。２桁の数字。先頭にゼロを付ける
#### `s`
*   秒。２桁の数字。先頭にゼロを付ける

### `date_default_timezone_set`
*   date_default_timezone_set関数を実行すると、スクリプト内で用いるタイムゾーンを設定します。この関数は設定に成功するとtrueを返し、失敗するとfalseを返しますが、今回のスクリプトでは戻り値を使用していません。一方、この関数を実行することで、「スクリプト内で用いるタイムゾーンが設定される」という副作用が得られます。<br>
    このように、関数の戻り値は使用せずに、関数を実行した際に得られる副作用だけを目的に、関数を呼び出す場合があります。また関数の中には、そもそも戻り値に意味がなく、常にNULLを返すものがあります。返り値を持たない関数は、その副作用だけを目的として呼び出します。

## rand関数
### 乱数
*   ランダムな数のことを乱数と呼びます。

### `rand()`
*   ランダムな数を取得するために用意されているのが、<mark>rand</mark>関数です。引数を指定せずにrand関数を呼び出すと、<mark>０以上、乱数の最大値以下の乱数</mark>を返します。

### `rand(最小値、最大値)`
*   最小値以上、最大値以下の乱数を生成します。

### `getrandmax()`
*   乱数の最大値は

#### 乱数の便利な使い方
*   rand()のような引数を渡さない場合、実行環境によっては、rand関数が生成する乱数の最大値が32767のような比較的小さい値のことがあります。もし、こういった環境において、例えば０以上１０００００以下のように、広い範囲の乱数を生成したい場合には、rand(0,100000)のように、引数で乱数の範囲を指定します。<br>
    また、偶数や基数の乱数だけを生成したいときには、次のようにします。例えば２以上１０以下の、偶数の乱数だけを生成するには、rand(1,5)*2とすれば得られます。１以上９以下の奇数の乱数だけを生成するには、rand(1,5)*2-1とするか、rand(0,4)*2+1とすれば得られます。

## preg_match関数
### `preg_match(パターン、入力文字列)`
*   <mark>preg_match</mark>関数を利用することで、正規表現によるパターンマッチングが行えます。
*   <mark>引数に指定したパターンに入力文字列がマッチすると、preg_match関数は「１」を返します。マッチしなかった場合には、「０」を返します。</mark><br>
    if文などの条件式でpreg_match関数を使った場合、「１」はtrue、「０」はfalseとして扱われます。０以外の整数は、すべてtrue扱いになります。<br>
    引数のパターンは<mark>正規表現</mark>を用いて記述します。7桁のハイフンなしの郵便番号を表すパターンは、^[0-9]{7}$のように記述できます。
```
    ^       :行頭
    [0-9]   :0から9までの数字１文字
    {7}     :直前の文字が7文字続く
    $       :行末
```
*   つまりこのパターンは、<mark>行頭から行末まで、0から9までの数字が7文字続く、</mark>という形式を表しています。このパターンを'/と/'で囲んで、'/^[0-9]{7}$/'のようにした文字列を、preg_match関数の引数に指定します。

### 正規表現の変更
*   前述とは異なり、7桁ハイフンありの郵便番号を表すパターンは以下の通りです。
```
    preg_match('/^[0-9]{3}-[0-9]{4}$/',$postcode)
```
```
    ^       :行頭
    [0-9]   :0から9までの数字を1文字
    {3}     :直前の文字が3文字続くことを表す
    -       :ハイフン
    [0-9]   :0から9までの数字1文字
    {4}     :直前の文字が4文字続くことを表す
    $       :行末
```

### パスワードの正規表現
*   パスワードの判定に用いる正規表現のパターンは以下のように少し複雑です。
```
    preg_match('/^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9][a-zA-Z0-9]{8,}$/',$password))
```
```
    ^           :行頭
    (?=.*[a-z]) :英小文字(aからz)を含む
    (?=.*[A-Z]) :英大文字(AからZ)を含む
    (?=.*[0-9]) :数字(0から9)を含む
    [a-zA-Z0-9] :英小文字、英大文字、数字のいずれか1文字
    {8,}        :直前の文字が8文字以上続く
    $           :行末
```

### 正規表現
*   パターンに含まれる「<mark>.</mark>」と「<mark>\*</mark>」です。「.」は任意の1文字を表します。「\*」は直前の文字の0回以上の繰り返しです。両者を組み合わせた「<mark>.*</mark>」は、任意の文字を0文字以上繰り返すことを表します。<br><mark>[]</mark>で囲まれた部分は<mark>文字クラス</mark>と呼ばれ、文字の集合を表します。「<mark>-</mark>」で範囲を表すことができます。例えば<mark>[a-z]</mark>は、1文字の英小文字を表します。<br><mark>[0-9]</mark>は1文字の数字を表しますが、<mark>¥d</mark>という簡略な記法も用意されています。この記法を使うと、ハイフンなしの7桁の郵便番号を表す正規表現は<mark>^¥d{7}$</mark>のように書けます。
ハイフンありの場合は<mark>^¥d{3}-¥d{4}$</mark>です。

## mb_convert_kana関数
*   <mark>mb_covert_kana</mark>関数は、PHPのマルチバイト文字列関数の一種です。マルチバイト文字列関数とは、日本語の文字など、コンピュータの内部で複数のバイトを使って表現する文字列に対して、いろいろな機能を提供する関数です。マルチバイト文字列関数は、関数名の先頭に「mb」が付いています。
*   mb_convert_kana関数を次のように呼び出すと、<mark>文字列に含まれる半角カタカナを、全角カタカナに変換します。</mark>
```
    mb_convert_kana(文字列)
```
*   半角カタカナで濁点や半濁点を使うと2文字を使いますが、全角カタカナに変換した際には1文字にします。例えば半角で2文字の「ﾋﾟ」は、全角で1文字の「ピ」に変換します。

### 数字の変換
*   カタカナの変換に用いた<mark>mb_convert_kana</mark>関数を用いて、全角数字を半角数字に変換することができます。この場合は引数を2個にして、以下のように記述します。
```
    mb_convert_kana(文字列、オプション)
```
*   全角数字を半角数字に変換するには、オプションにnを指定します。<mark>オプションを指定することで、様々な変換が行えます。</mark>
```
    mb_convert_kana($_REQUEST['count'],'n');
```
*   mb_convert_kana関数のオプション

|オプション|意味|
|:---|:---|
|r|全角英字を半角に変換|
|R|半角英字を全角に変換|
|n|全角数字を半角に変換|
|N|半角数字を全角に変換|
|a|全角英数字を半角に変換|
|A|半角英数字を全角に変換|
|s|全角スペースを半角に変換|
|S|半角スペースを全角に変換|
|k|全角カタカナを半角カタカナに変換|
|K|半角カタカナを全角カタカナに変換|
|h|全角ひらがなを半角カタカナに変換|
|H|半角カタカナを全角ひらがなに変換|
|c|全角カタカナを全角ひらがなに変換|
|C|全角ひらがなを全角カタカナに変換|
|V|濁点付きの文字を1文字に変換。KやHと併用する|

### 正規表現による数値の確認
*   数値が入力されたかどうかを、正規表現を用いて判定します。数値を表すパターンは^[0-9]+$のように記述できます。
```
    ^       :行頭
    [0-9]   :0から9までの数字1文字
    +       :直前の文字が1文字以上続く
    $       :行末
```
*   つまり、「0から9までの数字が1文字以上続く」ことを表しています。
```
    if(preg_match('^/[0-9]+$/', $count)) {}
```
*   $countは半角数字に変換された数値です。preg_match関数の戻り値をif-else文で判定して、パターンにマッチした場合には購入メッセージを表示し、マッチしなかった場合にはエラーメッセージを表示ます。

## JSON
*   メッセージをファイルに保存する際には、<mark>JSON</mark>という形式を使います。JSONは「ジェイソン」と読みます。JSONはJavaScript Object Notationの略です。
*   Jsonはプログラミング言語のJavaScriptにおける表記法に由来しますが、JavaScriptに限らず、いろいろなプログラミング言語で利用されています。PHPにおいてJSONを使う利点は、文字列や配列といったデータ構造を簡単にファイルに書き込んだり、読み込んだりできることです。<br>今回は、PHPスクリプトと同じフォルダにある<mark>board.txt</mark>というテキストファイルに、メッセージの一覧を保存します。

```
$file='board.txt';
if (file_exists($file)) {
    $board=json_decode(file_get_contents($file));
}
$board[]=htmlspecialchars($_REQUEST['message']);
file_put_contents($file,json_encode($board));
foreach ($board as $message) {
    echo '<p>', $message, '</p><hr>';
}
```

### file_exists関数
```
file_exists(ファイル名)
```
*   ファイルを読み込むための関数。ファイルが存在するか調べます。
*   file_exists関数は、<mark>指定したファイルが存在する場合にはtrue、存在しない場合にはfalseを返します。</mark>

### file_get_contents関数
`file_get_contents(ファイル名)`
*   flie_get_contents関数は、ファイルの全体を読み込み、内容を文字列として返します。

### json_decode関数
`json_decode(文字列)`
*   読み込むファイルは、JSON形式で保存されているので、PHPで扱える形式に変換する必要があります。そこで、JSONの入力を行う<mark>json_decode</mark>関数を使用します。
*   json_decode関数は、JSON形式の文字列を解釈して、PHPの文字列や配列といったデータに変換します。ここでは、file_get_contents関数で取得した文字列を、json_decode関数に渡します。

#### JSONファイルの読み込みは、存在確認、文字列の取得、文字列の変換の順番で行います。

### json_encode関数
`json_encode(値)`
*   メッセージを保存するために、ファイルに書き込みを行います。メッセージの配列をJSON形式に変換するために<mark>json_encode</mark>関数をつかいます。
*   値の部分には、変数や式を指定することができます。

### file_put_contents(ファイル名, 文字列)
*   file_put_contents関数は、指定した文字列を、指定したファイルに書き込みます。
*   file_put_contents関数は、書き込みを行うファイルが存在しない場合には、新規にファイルを作成します。存在する場合には、既存のファイルを上書きします。

#### 数値の保存
*   教科書ではメッセージの配列、つまり文字列の配列を保存しましたが、他の形式のデータを保存することも可能です。同様にjson_encode関数やfile_put_contents関数を使って、例えば数値の配列を保存することもできます。

## アップロードされたファイルの確認
*   <form>タグのファイル選択欄からアップロードされたファイルは、一時的なファイルに保存されます。この一時的なファイルのファイル名は、以下の記述で取得できます。
```
$_FILES['file']['tmp_name']
```
*   <mark>$_FILES</mark>は、PHPによって事前に用意された変数です。<mark>file</mark>は入力画面用のスクリプトでファイル選択欄につけた名前です。<mark>tmp_name</mark>を指定することで、一時的なファイルの名前が取得できます。
*   ここで取得した一時的なファイルが、<mark>入力画面からアップロードされたファイルかどうかを調べます。</mark>is_uploaded_file関数を使います。

### `is_uploaded_file(ファイル名)`
*   アップロードされたファイルの場合、is_uploaded_file関数はtrueを返します。ここではif文と組み合わせて、本当にアップロードされたものだった場合にのみ処理を行っています。
```
if (is_uploaded_file($_FILES['file']['tmp_name'])) {}
```

### 一時的なファイル
*   ブラウザでファイルをアップロードすると、ファイルの内容がサーバサイドに送信されます。PHPは受信したファイルの内容を、サーバサイドの一時的なファイルに保存します。スクリプトが終了すると、このファイルは自動的に削除されます。
*   PHPは一時的なファイルに対して、元のファイル名とは異なる名前を付けます。この名前はtmp_nameで取得できます。後述するように、元のファイル名はnameで取得できます。

### is_uploaded_file関数の意味
*   is_uploaded_file関数は、指定したファイルがアップロードされたファイルかどうかを確認します。この確認はセキュリティのためです。例えば、スクリプトが重要なファイルを操作するように仕向ける攻撃を防止する効果があります。

## フォルダの作成
*   アップロードされたファイルを保存するために、サーバ上にフォルダを作成します。まず、<mark>file_exists</mark>関数を用いて、保存策のフォルダが存在するかどうかを調べます。
```
file_exists(フォルダ名)
```
*   file_exists関数は、引数に指定したフォルダが存在する場合、trueを返します。存在しない場合、falseを返します。ここではフォルダが存在しない場合にフォルダを作成するため、以下のようなif文を記述します、フォルダ名はuploadとしました。
```
if(!file_exists('upload')){}
```
*   file_existsの前に付加した!は、trueとfalseを反転させる演算子です。<mark>!</mark>は論理演算子の一種で<mark>否定</mark>と呼ばれます。if文の式に「!」をつけることで、<mark>式の値がtrueではない場合は、</mark>という条件を書くことができます。<br>フォルダを作成するには、<mark>mkdir</mark>関数を使います。

### `mkdir(フォルダ名)`
*   フォルダを作成します。

## アップロードされたファイルの保存
*   アップロードされたファイルを保存します。まず、<mark>アップロードされたファイルの名前を取得して、保存先のファイル名を作成します。</mark>アップロードされたファイルのファイル名は、以下の記述で取得できます。fileは入力画面で設定したファイル選択欄の名前(name属性の値)です。
```
$_FILES['file']['name']
```

## `phpinfo()`
*   今動いているPHPの環境情報をすべて表示する

# 演算子

## 累乗演算子
### `**`

## インクリメント演算子
### `++`
*   変数に１を加算します

## デクリメント演算子
### `--`
*   変数から１を減算します

## 論理演算子（否定）
### `!`

## 乗算演算子
### `*`

## 除算演算子
### `/`

## 剰余演算子
### `%`

## 加算演算子
### `+`

## 減算演算子
### `-`

## 文字列結合
### `.`

## 比較演算子
### `<`
*   より小さい
### `<=`
*   以下
### `>`
*   より大きい
### `>=`
*   以上
### `==` 
*   等しい（型を参照しない）
### `===`
*   等しい（型まで参照する）
### `!=` 
*   等しくない（型を参照しない）
### `!==`
*   等しくない（型まで参照する）

## 論理演算子
### `&&`
*   かつ　もしくは　両方
### `||`
*   または　もしくは　どちらか

## 代入演算子
### `=`
*   代入演算子を使うと、左辺の変数に、右辺の値が書き込まれます。

# error 一覧

## Fatal error
*   致命的なエラー

### Uncaught error : 内容
*   キャッチされないエラー