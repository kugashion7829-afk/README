# C#

## C#の特徴、用語解説

### C#の文字列はダブルクォートで記述

### C#を使うには.NET SDKが必要

### 新しいプロジェクトを作成した時にターミナルに入力
dotnet new console

## 継承
継承は、あるクラスの機能（フィールドやメソッド）を、別のクラスが受け継ぐ仕組みです。受け継ぐ元を基底クラス（親クラス）、受け継ぐ側を派生クラス（子クラス）と呼びます。
#### なぜ継承を使うのか
例えば、今回のゲームでは「プレイヤー」と「敵」の両方に、共通する要素があります。

    HP（体力）

    ダメージを受ける処理

    移動やアニメーションの参照

これをそれぞれ別々に書くと、同じコードが2か所に存在することになります。1か所直すともう1か所も直さなければならず、修正漏れの原因になります。継承を使うと、共通部分を1か所にまとめ、各クラスは独自部分だけを書けば済むようになります。
### 派生クラス
クラス名の後に：<mark>基底クラス名</mark>を書くのが継承の構文です。

## C#の主な記述群

### MonoBehaviour
##### 分類：クラス
Unity上で利用できるCompornentとして設定する

### class
設計図らしい…

### static
実物を作らずに使える

### void 
戻り値を返さない

### sealed
##### 分類：修飾子
継承を禁止する

### abstract
##### 分類：修飾子
抽象。このクラス単体では使わず、必ず子クラスに継承させて使うことを示す目印です。abstract classは、それ自体をそのままGameObjectにアタッチできません。

### アクセス修飾子
publicだと外部全部に見え、privateだと子クラスからも見えなくなります。継承先にだけ見せたい場合はprotectedを使います。

#### public
##### 分類：アクセス修飾子
他のクラスから参照できる

#### private
##### 分類：アクセス修飾子
他のクラスから参照できない

#### protected
##### 分類：アクセス修飾子
このクラスと、それを継承した子クラスからだけアクセスできるアクセス修飾子です。

### Main()
##### 分類：メソッド
コンソールアプリの開始地点

### virtual
##### 分類：メソッド
仮想。このメソッドは子クラスで上書き(オーバーライド)していいですよ。という目印です。

### override
### Program 
##### 分類：クラス
    プログラム本体のクラス

### using ～～　
どの名前空間を使うのか指定する
SystemであればSystem名前空間にあるクラスを利用できる


#### using UnityEngine 
UnityEngineという名前空間を使用するという宣言

#### using System 
Systemという名前空間を使用するという宣言

### console
##### 分類：クラス
コンソールに関する機能を使用
---

#### console.WriteLine()
##### 分類：メソッド
コンソールに()内に格納されているものを一行表示する

#### console.Write()
##### 分類：メソッド
コンソールに()内に格納されているものを改行せず表示する

#### console.ReadLine()
##### 分類：メソッド
ユーザーが入力した値を受け取る

### Math : ~ ~
---

#### Mathf:Max
Unityの数学ヘルパーで、引数のうち大きい方を返します。ここでは「HPが0未満にならないように」使っています。

### 変数
---

#### string 変数名 = ""
string型は、キャラクター名やメッセージなど、任意の文字列を格納できます。C#では、文字列を「""」で囲います。
    string value = "ABCあいう123"

#### int 変数名 = ""
int型は、-2147483648～2147483647までの範囲の整数を格納できます。この範囲を超える整数を扱う場合はlong型を使います。

#### float 変数名 = ""
float型は、少数を格納できます。C#では、数値の末尾に「f」をつけることでfloat型であることを表します。桁数が多いと誤差が出る(値がほんの少しだけ意図せず変わってしまう)のが特徴で、誤差なく扱える有効桁数は6~9桁程度です。Unityは座標などの各所にfloat型を使っています。そのため、しばしば誤差が発生して、inspecterウィンドウに表示されている値が変わることがあります。
   float value = 0.25f;
   float value2 = 5f;

#### bool 変数名 = ""
bool型は、trueまたは、falseの２種類の値を格納できる一番シンプルな型です。    